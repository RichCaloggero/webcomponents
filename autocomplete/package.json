{
  "name": "autocomplete",
  "version": "1.1.0",
  "main": "autocomplete.component.js",
  "scripts": {
    "build": "browserify autocomplete.js -o autocomplete.component.js",
    "watch": "watchify autocomplete.js -o autocomplete.component.js -v"
  }
}

